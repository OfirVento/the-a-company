# HeyGen LiveAvatar API key — find it at https://app.liveavatar.com/developers
# Used only by the Vite dev-server proxy (vite.config.ts); never shipped to the browser.
LIVEAVATAR_API_KEY=your-api-key-here
